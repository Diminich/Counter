import React from 'react';
import './MaxStartValue.css'

class maxStartValue extends React.Component {

    render = (props) => {
        return (
            <div>
            </div>
        );
    }
};

export default maxStartValue;